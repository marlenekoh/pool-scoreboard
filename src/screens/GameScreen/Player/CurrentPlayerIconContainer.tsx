import styled from "styled-components/native";

export const CurrentPlayerIconContainer = styled.View`
  position: relative;
  left: -10;
  top: -3;
  transform: rotate(90deg);
  border-radius: 30;
`;
