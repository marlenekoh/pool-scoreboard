import React from "react";
import { View, Text } from "react-native";

export const TimerScreen: React.FunctionComponent = () => {
  return (
    <View>
      <Text>
        This is the timer screen. Show current player's score and shot clock
        here.
      </Text>
    </View>
  );
};
